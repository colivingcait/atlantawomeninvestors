/* =========================================================
   Atlanta Women Investors — UPCOMING MEETUPS
   ---------------------------------------------------------
   AUTO-SYNCED FROM EVENTBRITE. The daily GitHub Action
   (.github/workflows/eventbrite-sync.yml) rewrites this file
   from your live Eventbrite events, so the "next meetup" on
   the site always mirrors Eventbrite — change it there.

   You can still hand-edit this list as a fallback (e.g. before
   the sync is connected); the next sync will overwrite it.

   Each entry: { id, date:"YYYY-MM-DD", topic, summary, body,
                 speaker, eventbriteUrl }
   ========================================================= */
window.EVENTS = [
  // Populated from Eventbrite by the daily sync.
];
